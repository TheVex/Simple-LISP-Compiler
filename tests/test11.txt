(func Add (x y) (plus x y))
(func Multiply (a b) (times a b))
(func Square (n) (times n n))

(setq const1 (plus 100 50))
(setq const2 (minus 200 75))
(setq const3 (times 12 8))
(setq const4 (divide 144 12))

(setq nested (plus (times 5 4) (minus 30 10)))

(setq logic1 (and true true))
(setq logic2 (or false true))
(setq logic3 (not false))
(setq logic4 (xor true false))

(setq cmp1 (equal 10 10))
(setq cmp2 (less 5 10))
(setq cmp3 (greater 20 15))
(setq cmp4 (lesseq 5 5))

(setq type1 (isint 42))
(setq type2 (isreal 3.14))
(setq type3 (isbool true))

(setq result1 (Add 10 20))
(setq result2 (Multiply 5 6))
(setq result3 (Square 7))

(setq counter 10)
(while (greater counter 0)
  (setq counter (minus counter 1))
  (cond (equal counter 5)
    (break)
    (setq result1 (plus result1 1))))

(func TestReturn (x)
  (setq temp (plus x 100))
  (cond (equal x 0)
    (return 0)
    (setq temp (plus temp 50)))
  (return temp))

(prog ((local1 15) (local2 25))
  (setq sum (plus local1 local2))
  (setq product (times local1 local2))
  (return product))

(setq Cube (lambda (x) (times (times x x) x)))
(setq result4 (Cube 3))

(cond (equal (plus 2 2) 4)
  (setq success true)
  (setq success false))

(func Factorial (n)
  (cond (equal n 0)
    (return 1)
    (setq unused 999))
  (cond (equal n 1)
    (return 1)
    (setq another_unused 888))
  (return (times n (Factorial (minus n 1)))))

(setq mixed1 (plus 10 3.5))
(setq mixed2 (times 2.5 4))

(setq complex (and (less 5 10) (equal (plus 3 3) 6)))
